import 'package:flutter/material.dart';

class AddOnReadingPage extends StatelessWidget {
  const AddOnReadingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(body: Text('add an on reading')),
    );
  }
}